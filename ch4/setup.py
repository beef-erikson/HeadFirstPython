from setuptools import setup

setup(
    name='vsearch',
    version='1.0',
    description='A program to search for characters within a phrase',
    author='Troy Martin',
    py_modules=['vsearch']
)
